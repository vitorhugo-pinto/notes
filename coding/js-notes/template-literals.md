JS way to interpolate, mainly string. Using \`\` it's possible to pass variables and all other code conditions within the backticks.

```js
const name = 'Vitor';
const message = `Welcome, ${name}`;

console.log(message) // => Welcome, Vitor
```
